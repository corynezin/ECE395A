# ECE395A
Neural net implementation on an FPGA.
Brenda So*, Cory Nezin*
* Equal contribution


To compile with bibliography, do :
$ latex myfile.tex
$ bibtex myfile
$ latex myfile.tex
$ pdflatex myfile.tex
